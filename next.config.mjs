/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // All visuals in this prototype are generated SVG/CSS, so no remote image hosts
  // are configured yet. Add `images.remotePatterns` here when you plug in a CMS.
};

export default nextConfig;
